hit(
  id: "iterar-lista",
  name: "iterar-lista",
  scope: "demo"
) {
  step(
    hid: "main",
    navType: "push",
    lifecycle: {
      onCreate => {
        hit.set() -> (
          temp.lista: [
            { id: "1", nome: "Item A", valor: 10 },
            { id: "2", nome: "Item B", valor: 20 },
            { id: "3", nome: "Item C", valor: 30 }
          ],
          temp.listaProcessada: []
        )
      }
    }
  ) {
    blocks.scroll(
      width: "100%",
      height: "100%"
    ) {
      blocks.`icon-button`(
        accessibilityLabel: "Voltar",
        icon: "apl3_ic_arrow_left",
        onPress => {
          navigation.goBack()
        }
      )
  
      blocks.spacer(height: 16)
  
      blocks.title(
        title: "Iterar lista",
        variant: "normal"
      )
  
      blocks.spacer(height: 16)
  
      blocks.button(
        children: "Processar todos os itens",
        onPress => {
          hit.forEach(
            value: temp.lista
          ) {
            hit.insert(
              value: temp.listaProcessada,
              content: [fn.previous]
            ) -> (
              temp.listaProcessada: fn.result
            ) {
              hit.set() -> (
                listaDemo.data: temp.listaProcessada
              ) {
                hit.remount(
                  hid: "listaDemo"
                )
              }
            }
          }
        }
      )

      blocks.spacer(height: 8)
  
      blocks.list(
        hid: "listaDemo",
        trackBy: "id",
        disableFastRender: true,
        data: [],
        renderItem: render<blocks.`list-item`>(
          contentArea: {
            variant: "label+plus",
            label: item.nome,
            plus: item.valor
          }
        )
      )
    }
  }
}